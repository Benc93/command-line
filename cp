'cp' is a Unix command short for Copy
'cp' is used to copy files and directories. 
It takes 2 parameters. The first is the file to be copied, the second if the file it will be copied into
Example: cp file newFile (This will copy 'file' and recreate it exactly named 'newFile')
